<%- include('inc/top', { title: '게시글 삭제' }) %>

<style>
  .board-container {
    max-width: 800px;
    margin: 0 auto;
    padding: 20px;
  }
  
  .board-title {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 24px;
    color: var(--text);
    text-align: center;
  }
  
  .delete-warning {
    background: #fef2f2;
    border: 1px solid #fecaca;
    color: #dc2626;
    padding: 16px;
    border-radius: 10px;
    margin-bottom: 24px;
    text-align: center;
    font-weight: 600;
  }
  
  .form-table {
    width: 100%;
    border-collapse: collapse;
    margin: 0 auto;
    background: white;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    padding: 32px;
  }
  
  .form-table th {
    text-align: left;
    padding: 12px;
    font-weight: 600;
    color: var(--text);
    width: 100px;
  }
  
  .form-table td {
    padding: 12px;
  }
  
  .form-submit {
    text-align: center;
    padding-top: 20px;
  }
  
  .btn-danger, .btn-secondary {
    padding: 12px 24px;
    border-radius: 10px;
    font-weight: 600;
    cursor: pointer;
    border: none;
    font-size: 1rem;
    text-decoration: none;
    display: inline-block;
    margin: 0 8px;
  }
  
  .btn-danger {
    background: #dc2626;
    color: white;
  }
  
  .btn-secondary {
    background: #6b7280;
    color: white;
  }
  
  .btn-danger:hover, .btn-secondary:hover {
    opacity: 0.9;
  }
  
  .post-info {
    background: #f8fafc;
    padding: 16px;
    border-radius: 10px;
    margin-bottom: 24px;
    text-align: center;
  }
  
  .post-info p {
    margin: 4px 0;
    color: #666;
  }
</style>

<div class="board-container">
  <h2 class="board-title">게시글 삭제</h2>
  
  <div class="delete-warning">
    ⚠️ 정말 이 글을 삭제하시겠습니까?
  </div>
  
  <div class="post-info">
    <p><strong>작성자:</strong> <%= post.author || '알 수 없음' %></p>
    <p><strong>작성일:</strong> <%= new Date(post.createdAt).toLocaleString('ko-KR') %></p>
  </div>
  
  <table class="form-table">
    <tr>
      <th>제목</th>
      <td><%= post.title %></td>
    </tr>
    <tr>
      <th>내용</th>
      <td><%= post.body %></td>
    </tr>
    <tr>
      <td colspan="2" class="form-submit">
        <form method="post" action="/board/<%= post._id %>?_method=DELETE" style="display: inline;">
          <button type="submit" class="btn-danger">삭제</button>
        </form>
        <a href="/board" class="btn-secondary">취소</a>
      </td>
    </tr>
  </table>
</div>

<%- include('inc/bottom') %>